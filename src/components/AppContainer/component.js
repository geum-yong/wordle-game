const AppContainer = () => {
  const appContainerElement = document.createElement("div");
  appContainerElement.classList.add("appContainer");

  return appContainerElement;
};

export default AppContainer;
