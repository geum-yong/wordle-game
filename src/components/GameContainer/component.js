const GamContainer = () => {
  const gameContainerElement = document.createElement("main");
  gameContainerElement.classList.add("gameContainer");

  return gameContainerElement;
};

export default GamContainer;
